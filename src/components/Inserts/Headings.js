import React from 'react';
import styled from 'styled-components';

const Headings = ({ children, title }) => {
    //
};

const HeadingTwo = style.div`
margin: 2rem 0;
  .underline {
    width: 5rem;
    height: 5px;
    background: var(--clr-primary-5);
  }
`

export default Headings;
